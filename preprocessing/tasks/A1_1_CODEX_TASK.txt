当前任务：A-1.1｜2025 C题统一预处理落地与数据审计复现

你是A路线的工程实现助手。本轮只实现已经确定的预处理规范，不选择模型、不拟合问题1—4、不写摘要或论文。仅使用本地《C题.pdf》《附件.xlsx》和本任务单，不读取B的解答，不搜索往届解题方案。不要改动原始输入文件或现有工程的其他无关文件。

一、建立可追溯的数据层
读取全部工作表及A—AE列。保留原始字段与值，记录source_sheet、Excel行号、原序号以及record_id。以孕妇代码作为subject_id，保留其所有观测。
女胎U、V列即使表头为空，也按题面字段位置记录为Y染色体Z值和Y染色体浓度；保留全空，不填0。

二、只做明确、确定性的整理
1. 孕周解析为gest_week：11w+6=11+6/7，20w=20，兼容W/w；天数须为0—6。解析失败列入问题清单，不能猜测。题面“通常10—25周”不作为自动删除条件。
2. 日期按原始类型分别解析：Excel日期序号、YYYYMMDD、文本日期、空白。保留原值，无法解析则标记；不要据末次月经自动覆盖记录孕周。
3. 使用原始BMI。另算bmi_recomputed=体重/(身高/100)^2做核对。仅在原BMI缺失且身高体重有效时，在bmi_used中恢复并记录bmi_imputed及来源；其他已有值不直接替换。
4. 男胎y_pass：Y非缺失时1(Y>=0.04)；若原值缺失，标签也缺失。
5. 女胎abnormal：严格按题面AB非空为1、空白为0。保留AB原文；识别T13/T18/T21多标签。未知非空字符串标记人工核对，不猜类型。AE“胎儿是否健康”不作本轮异常标签，也不得充当未来分类输入。
6. P列总体GC是否在[0.40,0.60]只建qc_gc_flag，不自动删除记录；不擅自把该范围当成所有染色体GC列统一的删除阈值。X浓度为负也不截断、不自动删除。
7. 怀孕次数“≥3”保留为有序类别3次及以上，不转成精确次数3。

三、重复记录与字段不一致
以subject_id+检测抽血次数建立candidate_draw_key，只是候选组，不能认定为已确认的技术重复。统计这些组的记录数、日期数、孕周数，以及其他关键字段差异。另查同一对象的身高/年龄变动，输出清单。
本阶段不按subject_id去重，不平均候选重复测序记录，不删除异常标记行。区分完全重复与同一个人多次观测。

四、输出
- scripts/prepare_data.py：一条明确命令可重跑；保存输入文件SHA-256。
- data/processed_male.csv、data/processed_female.csv：保留原字段及派生列，UTF-8-SIG。
- reports/DATA_CHECK.md：规模、对象数、缺失、孕周/BMI范围、标签分布、候选重复组、全部处理规则与处理前后数量。
- reports/DATA_ISSUES.csv：定位到原表、行、字段、问题类型、处理状态。
- reports/RUN_INFO.md：实际运行命令、解释器路径、Python和依赖版本；不要声称未实际运行的检查已经通过。

参考核验数值（必须自己计算，发现不一致就解释，不硬改）：
男胎1082条、267个孕妇代码；女胎605条、147个孕妇代码。
男胎Y>=0.04为937条；女胎AB非空67条、空白538条。
女胎BMI缺失1条；女胎Y两列结构性全空。
按孕妇代码+抽血次数，多行候选组男胎40组/101行，女胎32组/83行。
P列总体GC不在[0.40,0.60]：男胎451行、女胎220行。
X浓度负值：男胎74行、女胎405行。
原始Excel SHA-256：14827156218bd4f7e4f16db4aa6d9f757c6648379e038ae6c6b58383648614af

五、停止条件
完成真实运行、输出上述文件和简短核验结果后停止。不要开始OLS、MixedLM、GEE、分类、分组优化或论文写作。下一步模型拟合需要A明确批准。
