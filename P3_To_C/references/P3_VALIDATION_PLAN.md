# P3 验证计划

## A. 本轮A-4：限定小例与模型完整性

A-4不拟合官方历史、不运行一天或全年真实策略、不导出工作簿。可只读检查源字段/哈希/首末时间、引用、模板合同和一月状态来源；不再次做全年的预测指标。运行所附35项人工小例，8次微型LP。Codex另外从不同写法独立核对下述最少6组逻辑，最多新增12次微型LP；不能用同一函数再调用一次充当独立验证。

1. 100→80普通费90、100→150费175、100→0费50；取消与unused不同；两张费用列不相加。
2. 100→150→80：主合同90，逐次备选140；不能以费用更低改主合同。
3. 无储能经验均匀0..100的单槽：0时最优80，g0=100调整90，g0=40调整70，g0=80保持80；离散平坦区可有多解，应以目标值而非强行逐位唯一性判断。
4. KEEP是LP可行特例；LP目标不增不代表实际执行评分不增。费用一次入账且未来假想量不导出。
5. 共享C,D没有场景索引；去循环保持SOC且不增加短缺成本；实际反馈不同时充放，紧急不主动充电。
6. 反馈未来扰动保持此前动作不变；整点规划不能见当前未结束槽；非负参数/缺失拒绝，不将NaN设0。

源字段不符、结算冲突、数学不成立或信息集无法实现：BLOCKED。已披露的理论近似只能作为限制，不能伪称闭环真实最优。

## B. 后续A-5：必须验证，当前不执行

### B1. 来源与时间

官方和三个Common表哈希；P3-0合同及两个审计Gate；发行1460、原小时35040、10分钟发行目标210240（其中216跨实际边界）；正式334×144。发行、目标、lead、同日/次日分离；0/6/12/18边界按0/36/72/108完成槽。

### B2. 预测与训练

逐发行记录训练标签最大结束时刻≤信息截止；前缀只用已结束槽；W1源日为target−7且可用；winsorize阈值、αβ均由对应历史训练集产生；不足样本回退而不接触未来。对审计中的`future_*`列实行禁止作为当前特征的断言。不得把描述性全年斜率、分位或月均值变成参数。

每条残差使用已发布的历史预测版本；不能按当前回归重新生成旧预测。场景来源同issue hour、最近28、至少14、整条24h成熟；用负PV尾对应短缺，检查净残差eL−ePV。非负截断有记录。

### B3. 两层非预知性

①真实信息集：固定3月20日、7月1日、12月9日的0/6/12/18，修改该时点之后actual及未来发行预报，当前预测、训练参数、场景成员、初始计划/修订候选和决定不变；修改最近合法前缀作正对照。

②情景执行：固定每个候选向量，扰动某情景未来槽，此前反馈动作不变。只允许当前场景状态递推；不对每条路径重新求知道整条未来的“最优电池动作”。逐情景自由追索即使不读真实future，也不能被当作当前因果执行器。

### B4. 计划与状态

每策略2月1日统一预热初态，此后各自连续；6/12/18用自身actualSOC。历史承诺/动作不可变；g0页不覆盖；next-day hypothetical q不写正式承诺。KEEP与ADJUST同信息、同场景、同终端评估，门槛只为数值容差，不按年度费调优。

### B5. 实际物理

全量SOC范围、每槽功率、0.9效率方向、递推、跨日连续、无日重置；C×D≈0；E、U、Wpv≥0；used≤a；E>0不主动充电。能量式：used+E+PV+D=Load+C+Wpv。同一槽a=used+U；名义z不能当实际E。

### B6. 账单

四项逐槽独立重算；初始报价只参考；版本路径不重复结算；a未取用仍照付；取消不是售电。普通账F与p[a+.5abs(a−g0)]等价。参考LP、回放评分、终端价值、假想次日费用与真实账单分别存，不混加。

### B7. 导出

按P3-0cell map逐格核对两张335×147表：g0/最终a及EP/EQ；调整页EQ包括真实紧急费；4h实际2004条、首末SOC各334、同日相邻正紧急槽合并事件。全日不调整时a=g0，不填0。保护原始模板与P2result2的哈希。

### B8. 实验与评价

八机会集合全部同协议；MAIN不重复跑；11政策矩阵不可因结果不佳静默删除。预测指标用共同样本、说明单位，白昼/当日/延续分开；样本重复发行不能当独立统计样本。年度最便宜集合只能作事后探索，不能回填早期决策。

成本改善、紧急风险下降、鲁棒性增强是不同结论。有限样本最大费用降低不能宣称分布外保障。终端压力扣减另列，不改真实总账。P2对比保留结构差异，旧oracle非P3认证下界。

## C. 故障与恢复

小例不通过只修实施缺陷，不能趁机换算法。A-5预留按策略按日原子checkpoint、manifest和调用计数；失败输出BLOCKED或PAUSED，不以0替代。新增参数/方法搜索需要显式重新授权，保留旧证据。
